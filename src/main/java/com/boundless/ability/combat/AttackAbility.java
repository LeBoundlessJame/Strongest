package com.boundless.ability.combat;

import net.minecraft.entity.Entity;
import net.minecraft.entity.LivingEntity;
import net.minecraft.entity.player.PlayerEntity;

import java.util.function.BiConsumer;

public class AttackAbility {
    /*
    public BiConsumer<PlayerEntity, Entity> attackEnemyLogic = (player, enemy) -> {
        enemy.damage(damage);
    };

    public void performAttack() {
        if (attackEnemyLogic != null) forEach(attackEnemyLogic);
    }

    public void forEach(BiConsumer<PlayerEntity, Entity> logic) {
        for (LivingEntity target : heroAction.getWorld().getEntitiesByClass(LivingEntity.class, heroAction.getBoundingBox(), entity -> true)) {
            if (target != )
        }
    }

     */
}
